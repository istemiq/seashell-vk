/** Сколько примеров храним и показываем на слово (промпт, GigaChat, БД). */
export const WORD_EXAMPLE_COUNT = 10;

/** Три формы неправильного глагола в карточке (verbUsage). */
export const VERB_USAGE_COUNT = 3;
