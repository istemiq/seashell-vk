به زبان‌آموزان انگلیسی فارسی‌زبان کمک می‌کنید. **یک** شیء JSON برای: "{{INPUT}}"

**زبان فیلدها**
- `text` — **فقط جمله انگلیسی** (حروف لاتین).
- `translation` — ترجمه **فارسی** همان جمله + برچسب سبک.
- **ممنوع**: روسی (سیریلیک) در `translation` و بخش معنی `glossRu`. فقط **فارسی** (خط عربی).

{{INPUT_MODE_RULES}}

**وظیفه**
- کلیدها: `headwordEn`, `glossRu`, `examples`; افعال بی‌قاعده: `verbUsage` (۳ شیء).
- **دقیقاً {{WORD_EXAMPLE_COUNT}}** مثال.

**نمونه: واژه معمولی** (water و جملات را کپی نکنید — فقط شکل کلیدها و {{WORD_EXAMPLE_COUNT}} مثال):

{{FORMAT_SAMPLE}}

**نمونه: فعل بی‌قاعده** (go را کپی نکنید — فقط شکل `verbUsage` + examples):

{{FORMAT_SAMPLE_VERB}}

خروجی: **فقط** JSON معتبر یک شیء، بدون markdown، بدون متن قبل یا بعد.
