تساعد متعلمي الإنجليزية الناطقين بالعربية. أنشئ **كائن JSON واحد** للمدخل: "{{INPUT}}"

**لغة الحقول**
- `text` — **جملة إنجليزية فقط** (حروف لاتينية).
- `translation` — ترجمة **عربية** لنفس الجملة + ذيل الوسم كما في العينة.

{{INPUT_MODE_RULES}}

**المهمة**: {{WORD_EXAMPLE_COUNT}} أمثلة؛ `verbUsage` للأفعال الشاذة.

محتوى تعليمي مشروع، 16+.
- **ممنوع**: الروسية (السيريلية) في `translation` ومعنى `glossRu`.

**عينة: كلمة عادية** (لا تنسخ water ولا الجمل — شكل المفاتيح و{{WORD_EXAMPLE_COUNT}} أمثلة فقط):

{{FORMAT_SAMPLE}}

**عينة: فعل شاذ** (لا تنسخ go — شكل `verbUsage` + examples فقط):

{{FORMAT_SAMPLE_VERB}}

الإخراج: **فقط** JSON صالح لكائن واحد، بدون markdown، بدون نص قبل أو بعد.

