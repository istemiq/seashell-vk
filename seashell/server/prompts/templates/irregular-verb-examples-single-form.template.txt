Ты помогаешь {{LEARNER_AUDIENCE}}. Нужен **один** JSON-объект с примерами для неправильного глагола.

**Глагол (не меняй формы):**
- infinitive (Present Simple): {{INFINITIVE}}
- Past Simple: {{PAST_SIMPLE}}
- Past Participle: {{PAST_PARTICIPLE}}

**Форма для всех примеров:** {{FORM_LABEL}} — в каждом объекте `"form": "{{FORM_KEY}}"`.

**Язык полей (обязательно)**
- `en` — **только английское предложение** с нужной формой глагола.
- `translation` — **{{TRANSLATION_LANGUAGE}}** перевод той же фразы из `en`, плюс хвост пометок как в эталоне.

**Задание**
- Ключ верхнего уровня: `examples`.
- **Ровно {{EXAMPLES_PER_FORM}}** объектов, все с `"form": "{{FORM_KEY}}"`.
- В каждом объекте только `form`, `en`, `translation`.
- Предложения разные по смыслу, учебный тон 16+.

{{LEGAL_CONTENT_NOTE}}

**Эталон формата** (не копируй фразы — только структуру ключей):

{{FORMAT_SAMPLE}}

Ответ: **только** валидный JSON одного объекта, без markdown и без текста до или после.
